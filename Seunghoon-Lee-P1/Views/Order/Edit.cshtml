@model Seunghoon_Lee_P1.Models.DomainModels.LineItem

@{
    ViewData["Title"] = " | Edit Cart";
}

<h1>Edit Line Item</h1>
<form asp-controller="Order" asp-action="Edit" method="post">
    <ul class="list-group mb-2">
        <li class="list-group-item">@Model.Product.Name</li>
        <li class="list-group-item">
            <div class="col-1">
                <label>Price:</label>
            </div>
            <div class="col-1">
                @Model.Product.Price.ToString("c")
            </div>
        </li>
        <li class="list-group-item">
            <div class="form-row">
                <div class="col-1">
                    <label asp-for="Quantity">Quantity:</label>
                </div>
                <div class="col-1">
                    <select asp-for="Quantity" class="form-control">
                        @for (int i = 1; i <= 10; i++)
                        {
                            <option>@i</option>
                        }
                    </select>
                </div>
            </div>
        </li>
    </ul>
    <input type="hidden" asp-for="Product.ProductId" />
    <input type="hidden" asp-for="Product.Name" />

    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-controller="Order" asp-action="Index" class="btn btn-primary">Cancel</a>
</form>
